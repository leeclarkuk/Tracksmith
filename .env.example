PORT=3000
HOST=127.0.0.1
GATEWAY_URL=http://localhost:5476
GATEWAY_TOKEN=
# Optional API auth for split dev (web :5173, API :3000). Leave empty for same-origin production.
# Do NOT set VITE_TRACKSMITH_API_TOKEN in public deployments; use single-port serve without API token.
TRACKSMITH_API_TOKEN=
CORS_ORIGIN=http://localhost:5173
DATABASE_PATH=./data/tracksmith.db
